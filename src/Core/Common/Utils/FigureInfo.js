export default class FigureInfo {
    vertices;
    countVertices;
    width;
    height;
    depth;
    constructor(vertices, countVertices, width, height, depth) {
        this.vertices = vertices;
        this.countVertices = countVertices;
        this.width = width;
        this.height = height;
        this.depth = depth;
    }
}
//# sourceMappingURL=FigureInfo.js.map