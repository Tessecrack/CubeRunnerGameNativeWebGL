export default class CollisionBox {
    width;
    height;
    depth;
    constructor(width, height, depth) {
        this.width = width;
        this.height = height;
        this.depth = depth;
    }
}
//# sourceMappingURL=CollisionBox.js.map